# Database (SQLite - default for local dev)
DATABASE_URL="file:./prisma/dev.db"

# API keys for AI (Gemini, etc.) are stored locally in the app only (Settings).
# Do not add them here.
# OPENAI_API_KEY=

